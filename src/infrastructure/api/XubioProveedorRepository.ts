import { XubioBaseRepository } from "./XubioBaseRepository.js"; import { IProveedorRepository } from "../../domain/repositories/IProveedorRepository.js"; import { Proveedor } from "../../domain/entities/Proveedor.js"; export class XubioProveedorRepository extends XubioBaseRepository implements IProveedorRepository { async findAll(): Promise<Proveedor[]> { return await this.get("ProveedorBean"); } }
