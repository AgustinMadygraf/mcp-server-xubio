import { XubioBaseRepository } from "./XubioBaseRepository.js"; import { IProductoRepository } from "../../domain/repositories/IProductoRepository.js"; import { Producto } from "../../domain/entities/Producto.js"; export class XubioProductoRepository extends XubioBaseRepository implements IProductoRepository { async findAll(params?: any): Promise<Producto[]> { return await this.get("ProductoVentaBean", params); } async findById(id: string | number) { return await this.getById("productoBean", id); } }
