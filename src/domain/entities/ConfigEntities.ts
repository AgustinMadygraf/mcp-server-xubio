export interface Moneda { ID: number; nombre: string; codigo: string; simbolo?: string; }
export interface Pais { ID: number; nombre: string; codigo: string; }
export interface Provincia { ID: number; nombre: string; codigo: string; }
export interface Localidad { ID: number; nombre: string; codigo: string; }
export interface TasaIva { ID: number; nombre: string; porcentaje: number; codigo: string; }
export interface ActividadEconomica { ID: number; nombre: string; codigo: string; }
export interface UnidadMedida { ID: number; nombre: string; codigo: string; }
